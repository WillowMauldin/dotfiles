[date]
command=date +'%Y-%m-%d %H:%M:%S'
interval=1

{{- if eq .chezmoi.hostname "klefki" }}
[battery]
command=cat /sys/class/power_supply/BAT1/capacity | awk '{ print $1 "%" }'
interval=60
{{- end }}
